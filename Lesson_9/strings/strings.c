#include <stdio.h>
#include <stdlib.h>

                        //strings

int main() {
    int i = 0;
    char string[3] = {'Y','T','U'};

    for(i=0; i<3; i++){

    printf("%c",string[i]);

    }
    return 0;
}