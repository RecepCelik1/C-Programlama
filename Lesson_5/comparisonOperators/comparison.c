#include <stdio.h>
#include <stdlib.h>

                    // Comparison Operators

int main()
{

    /*
        Comparison Operators

        ==  !=  >   <   >=  <=
    */

    int x = 15, y = 20;

    printf("%d", x == y);   // == "eşit mi?" operatörüdür. eğer eşitse true yani 1 değerini döndürür değilse false (0) döndürür.
                            // != eşit mi operatörünün tersidir.
    
    return 0;
}
